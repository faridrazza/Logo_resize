Reproduce the supplied image exactly as it is.

The supplied image is already finished. It is a {canvas_size} x {canvas_size} square
showing a logo of {content_width} x {content_height} pixels resting at its final
position, with its top-left corner at x={content_left}, y={content_top}, on a solid
{background_hex} plate. The composition is correct and complete.

Return that same image.

The logo has already been scaled and centred before you received it. There is no
resizing to perform, no centring to perform and no layout to decide. Your output
differs from the input in nothing at all.

The artwork is photographic content, not a design to interpret. Every mark inside
the {content_width} x {content_height} region is fixed: the exact letterforms and
their exact edges, the exact stroke weights, the exact colours and colour values,
the exact spacing between characters and elements, the exact icon geometry. Copy
the pixels; do not read the artwork, do not identify what it depicts, and do not
reconstruct it from what you recognise. If any text appears, it is shape, not
language.

The plate behind the logo is flat {background_hex} and stays flat {background_hex}
to all four edges. It is a single uniform colour with nothing on it.

Output one image, {canvas_size} x {canvas_size} pixels, PNG.
